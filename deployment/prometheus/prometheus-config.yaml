apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: autoscaling-ns
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
    - job_name: 'autoscaler-app'
      static_configs:
      - targets: ['autoscaler-app:9090']